# AGENTS.md

## Codex への指示

変更を加えたときは、必ず `README.md`、`package.json`、`spec/`、`doc/` とドリフトが出ていないか確認すること。
必要な変更がそれらに含まれる場合は、意図した差分だけが残っているかもあわせて確認すること。
配布手順やセットアップ手順を `doc/` に追加・更新した場合は、README からその手順書へ辿れる導線も同時に整えること。
エンドユーザー向けの使い方は、開発手順とは分けて、README に最小限の起動・利用手順を直接書くこと。
`doc/` の文章は、単なる仕様説明ではなく、利用者から見える見え方・使い方・操作の流れを優先して書くこと。
`doc/` にはユーザーとの会話、調査に至った経緯、想定される反論への弁明を持ち込まず、確認できた結果を文書として自然に直接書くこと。
開発環境は Windows ネイティブを正本とし、ソースコード、依存関係、ビルド、動作確認を Windows ファイルシステム上で扱うこと。WSL 専用の手順や回避処理は追加しないこと。
